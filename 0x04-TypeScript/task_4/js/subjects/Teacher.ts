// In TypeScript, a namespace is a way to group related code together, making it easier to organize and avoid name collisions. 
namespace Subjects {
    export interface Teacher {
        firstName: string;
        lastName: string;
    }
}